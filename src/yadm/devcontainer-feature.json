{
    "name": "Yet Another Dotfiles Manager (yadm)",
    "id": "yadm",
    "version": "1.0.3",
    "description": "Install yadm (Yet Another Dotfiles Manager) for managing dotfiles in a git repository",
    "documentationURL": "https://yadm.io/",
    "options": {
        "repositoryUrl": {
            "type": "string",
            "default": "",
            "description": "Git repository URL to clone with yadm. If provided, 'yadm clone <url>' will be executed after installation."
        },
        "localClass": {
            "type": "string",
            "default": "",
            "description": "Yadm config local.class value. If provided, 'yadm config local.class <localClass>' will be executed after clone."
        },
        "overwriteExisting": {
            "type": "boolean",
            "default": false,
            "description": "If true, existing files will be overwritten when cloning the dotfiles repository using 'yadm checkout $HOME'."
        },
        "decryptOnClone": {
            "type": "boolean",
            "default": false,
            "description": "If true, run 'yadm decrypt' after clone when an encrypted yadm archive exists (e.g. $HOME/.local/share/yadm/archive). Requires GPG keys/agent available in the container."
        }
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils",
        "ghcr.io/devcontainers/features/git"
    ]
}
